import 'dart:ui';

class LightColorInfo {
  final String title;
  final String subtitle;
  final Color textColor;
  final Color chipColor;
  final Color lightColor;

  LightColorInfo(this.title, this.subtitle, this.textColor, this.chipColor, this.lightColor);
}
